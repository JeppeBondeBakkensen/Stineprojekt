# Test Project

A Python project using uv, ty, Ruff, pytest, and the Tokyo Night Storm VS Code
theme.

```shell
uv sync
uv run ruff check .
uv run ruff format --check .
uv run ty check
uv run pytest
```
